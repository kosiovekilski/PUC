menu()  // нот фин
Функцията не приема параметри и връща цяло число (1, 2, 3 или 4).
Извежда на екрана различните опции.
Потребителя 


is_empty_line()
Приема един аргумент line, масив от char (string). Не връща стойност.
Проверява дали в стринга има значещи символи, ако няма увеличава глобалната променлива ----- с 1


is_in_comment()
Приема един аргумент ---- цяло число. Връща 1 или 0.
Проверява дали ---- е нечетно, което значи че ----


operators_in_line()
Приема един аргумент line, масив от char (string). Не връща стойност.


is_c_file()
Приема един аргумент ---------, име на файл. Връща 1 когато е С файл и 0 когато не е.
Проверява последните два знака.

read_from_file()
Не приема аргументи. Не връща стойност.
Потребителят въвежда име на файл, ако не е С файл програмата спира.
Четене ред по ред


read_from_keyboard()
Не приема аргументи. Не връща стойност.

write_to_file()


void write_on_screen()


